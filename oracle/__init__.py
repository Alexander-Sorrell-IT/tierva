"""Tierva oracle bridge package.

The off-chain -> on-chain daemon that pushes consensus signal readings to a
deployed Pacto contract. See ``oracle.py`` for the pusher (chain/key-agnostic)
and the series provider.
"""
